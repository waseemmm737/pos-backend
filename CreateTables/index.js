var sql = require("mssql");
module.exports = function (sql) {
    // const table = new sql.Table('Users');
    // table.create = true
    // table.columns.add('email', sql.VarChar(200), { nullable: false, })
    // table.columns.add('name', sql.VarChar(200), { nullable: false })
    // table.columns.add('password', sql.VarChar(200), { nullable: false })
    // table.columns.add('username', sql.VarChar(200), { nullable: false })
    // table.columns.add('isAdmin', sql.Int, { nullable: false })
    // new sql.Request().bulk(table, (err, result) => {
    //     if (err) return console.log("Error",err)
    //     console.log("created", result)
    // })
}